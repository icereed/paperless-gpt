import linked from "@pgpt/ui-extension";
import type { UIExtension } from "./extension-api";

/** The UI extension compiled into this build (empty in the Community build). */
const extension: UIExtension = linked;
export default extension;
